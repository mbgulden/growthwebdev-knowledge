# Surface sweep — 2026-08-18

Context: after mirroring ned/george to kai's stack and removing llama-fred, Michael asked "what other cleanup for an optimal surface?"

## Reclaimed (executed, verified)
| Item | Box | Reclaimed |
|---|---|---|
| `profiles/fred/state.db` VACUUM (4.99G -> 2.9G; freelist was 1.88G) | local | 2.1 GB |
| `docker container prune -f` (exited containers) | local | 3.3 GB |
| npm cache clean x4 profiles (fred .npm 5.1G -> 468M etc.) | local | ~1.3 GB |
| Orphan k3s `llm-inference` ns on .230: 3 services (NodePorts 31001/31002/31003), 3 zero-replica deployments, 26 stale replicaset ghosts | .230 | confusion surface (disk via containerd GC later) |
| 15-byte `qwen2.5-7b-instruct-q4_k_m.gguf` (failed download) + empty `Qwen3.8-27B-W4A16` dir | .230, .232 | ~10 MB + clutter |

## Left alone (verified healthy)
- Local :8001 listener = hd-platform `vm_orchestrator.py` (cron ping is legit)
- .230 GPU0/1 VLLM workers = `vllm-fred.service` (Fred's INT8 backend, :8000)
- Local services autobot / becca-sage / belief-deprogrammer / beyondsaas-bot — all active+enabled, owner's bots
- k3s core on .230 (kube-system only after cleanup)
- .232 small models (0.5b/1.5b/coder-7b q4, 6.6G) — unverified usage

## Left to owner (reported, not acted)
- .230 `/models` unreferenced: `Qwen3.8-27B` fp16 52G + `qwen3.8-27b-q5` 20G + qwen2.5 smalls 6.6G ~ 78GB
- k3s full uninstall on .230 (~20GB + RAM)
- Old session dumps: ned 492 files >30d old (194M), kai sessions 317M

## Final disk state
- local (webtop-hermes): 214G/292G (74%)
- .230: 194G/291G (67%)
- .232: 56G/97G (57%)

## Technique notes
- state.db dead-space check: `PRAGMA freelist_count` x `page_size` vs `page_count` x `page_size`. fred 1.88G free / kai 0.03G — only vacuum the big ones.
- Profile fake-$HOME (`profiles/<p>/home/.npm`, `.cache`, `.local`) holds the bloat, not the visible profile subdirs.
- `du -sh /home/ubuntu/*` timed out at 120s — always `du -xsh` per top-level dir.
- `memory` tool batch ops mangled with nested quotes 3x; single-op calls fixed it.
