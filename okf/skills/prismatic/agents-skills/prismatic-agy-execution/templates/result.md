# Prismatic AGY Result

## Status

`PASS | PARTIAL | BLOCKED | FAIL`

## Problem

`${PROBLEM}`

## Changed

- `${CHANGE}`

## Why it matters

`${IMPACT}`

## Evidence

```text
COMMAND=${EXACT_COMMAND_OR_GROUPED_SUMMARY}
RESULT=${PASS_FAIL_BLOCKED}
LOG=${DURABLE_LOG_PATH}
SCOPE=${VERIFIED_SCOPE}
AD_HOC_OR_CANONICAL=${EVIDENCE_CLASS}
NOT_CLAIMING=${EXPLICIT_NON_CLAIMS}
HEAD=${COMMIT_IF_APPLICABLE}
TREE=${TREE_IF_APPLICABLE}
MARKER=${RESULT_MARKER}
```

## Artifacts

- Candidate: `${CANDIDATE_REF}`
- Result: `${RESULT_PATH}`
- Evidence: `${EVIDENCE_PATH}`

## Remaining risks or blockers

- `${RISK_OR_BLOCKER}`

## Next governed action

`${NEXT_ACTION}`

Completion does not authorize merge, deployment, restart, publication, external writes, or self-acceptance.
